# Plan 03-02: 投注内容与赔率解析、多注识别

**Phase:** 3 - Lottery Parsing
**Status:** Completed
**Created:** 2026-04-16
**Completed:** 2026-04-16

## Goal

实现投注内容（比赛、选项）解析、赔率提取、多注识别。

## Implementation

### 数据结构

```python
@dataclass
class Bet:
    match: str          # "阿根廷 vs 巴西"
    option: str         # "胜" / "平" / "负"
    odds: float         # 1.85
```

### 投注内容解析

```python
MATCH_PATTERN = re.compile(r"([\u4e00-\u9fa5]+)\s*vs\s*([\u4e00-\u9fa5]+)")
ODDS_PATTERN = re.compile(r"(\d+\.\d{2})")
OPTION_PATTERN = re.compile(r"(胜|平|负|主胜|客胜|主负)")
```

### 多注识别策略

1. 查找所有比赛（通过 `vs` 模式）
2. 对每个比赛，提取周围上下文
3. 在上下文中查找赔率和选项
4. 每场比赛创建一个 Bet 实例

### 核心代码

```python
def parse_bets(self, text: str) -> List[Bet]:
    bets = []
    match_matches = list(self.MATCH_PATTERN.finditer(text))

    for match_obj in match_matches:
        match_name = f"{match_obj.group(1)} vs {match_obj.group(2)}"
        context = self._get_context_around_match(text, match_obj.start())
        option = self._infer_option(context)
        odds = self._find_closest_odds(context, len(context) // 2)

        if odds > 0:
            bets.append(Bet(match=match_name, option=option, odds=odds))

    return bets
```

## Verification

```python
text = '''
阿根廷 vs 巴西
  胜:1.85  平:2.10  负:3.50
中国 vs 日本
  胜:1.65  平:2.30  负:3.20
'''
bets = parser.parse_bets(text)
assert len(bets) == 2  # 2 matches = 2 bets
```

---
*Plan completed: 2026-04-16*