# Plan 04-01: JSON/CSV Export Implementation

**Phase:** 4 - Output & CLI
**Plan:** 04-01
**Status:** Ready for implementation
**Requirements:** OUT-01, OUT-02, OUT-04

## Goal

实现 JSON/CSV 结构化数据导出，支持批量处理结果的多格式导出和原始图片文件名映射。

## Requirements Coverage

| Requirement | Description | Implementation |
|-------------|-------------|----------------|
| OUT-01 | JSON 结构化数据导出 | `Exporter.export_to_json()` |
| OUT-02 | CSV 表格数据导出 | `Exporter.export_to_csv()` |
| OUT-04 | 导出结果与原始图片文件名正确映射 | `ExportRecord.source_file` / `source_filename` |

## Design

### Data Model: ExportRecord

```python
@dataclass
class ExportRecord:
    source_file: str           # 完整路径
    source_filename: str       # 文件名
    issue_number: Optional[str]
    bet_time: Optional[str]
    total_amount: Optional[float]
    bets_count: int
    bet_details: str           # 格式: "主队 vs 客队 选项@赔率; ..."
    status: str                # "success" / "failed"
    error: Optional[str] = None
```

### Exporter Class

```python
class Exporter:
    def __init__(self, parser: Optional[LotteryParser] = None)
    def ocr_result_to_record(...) -> ExportRecord
    def export_to_json(records, output_path, include_raw=False, raw_data=None) -> None
    def export_to_csv(records, output_path) -> None
    def export_batch_result(batch_result, output_json=None, output_csv=None, include_raw=False) -> Dict
```

### JSON Output Format

```json
{
  "total": 10,
  "success_count": 8,
  "failed_count": 2,
  "records": [
    {
      "source_file": "/path/to/image.jpg",
      "source_filename": "image.jpg",
      "issue_number": "25042",
      "bet_time": "2026-04-16",
      "total_amount": 66.0,
      "bets_count": 2,
      "bet_details": "阿根廷 vs 巴西 胜@1.85; 中国 vs 日本 胜@2.10",
      "status": "success",
      "error": null
    }
  ]
}
```

### CSV Output Format

| source_file | source_filename | issue_number | bet_time | total_amount | bets_count | bet_details | status | error |
|-------------|-----------------|--------------|----------|--------------|------------|-------------|--------|-------|
| /path/...   | image.jpg       | 25042        | 2026-04-16 | 66.0         | 2          | 阿根廷...   | success | |

## Implementation

### File: `src/exporter.py`

- [x] `ExportRecord` dataclass 定义
- [x] `Exporter.ocr_result_to_record()` - OCR 结果转 ExportRecord
- [x] `Exporter.export_to_json()` - JSON 导出
- [x] `Exporter.export_to_csv()` - CSV 导出
- [x] `Exporter.export_batch_result()` - 批量结果导出

### File: `tests/test_exporter.py`

- [x] `TestExportRecord` - ExportRecord 数据类测试
- [x] `TestExporterOCRResultToRecord` - ocr_result_to_record 测试
- [x] `TestExporterJSONExport` - JSON 导出测试 (OUT-01, OUT-04)
- [x] `TestExporterCSVExport` - CSV 导出测试 (OUT-02, OUT-04)
- [x] `TestExporterBatchResultExport` - 批量导出集成测试

## Execution

```bash
# 运行测试验证
cd /Users/stephen/Projects/202604/ocrv1
uv run pytest tests/test_exporter.py -v
```

## Verification Criteria

1. JSON 导出文件格式合法，包含 total/success_count/failed_count/records
2. CSV 导出可用 Excel 正常打开，无编码问题
3. source_file 和 source_filename 正确映射原始图片路径
4. 测试用例全部通过

## Dependencies

- Phase 3: `src/parser.py` (LotteryParser, LotteryTicket, Bet)
- Phase 2: `src/batch.py` (BatchResult)

## Transition

**Next:** 04-02 - CLI Command Line Interface

Plan 04-01 is complete when JSON/CSV export works and tests pass.