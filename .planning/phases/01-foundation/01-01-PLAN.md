---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/__init__.py
  - scripts/check_env.py
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03, ENV-04]
---

<objective>

配置开发环境，安装 PaddlePaddle CPU 版本、PaddleOCR 和图像预处理依赖（opencv-python-headless, Pillow, NumPy）。

</objective>

<tasks>

<task>
<read_first>
- ./AGENTS.md
- .planning/research/STACK.md
</read_first>

<action>

1. 创建 `requirements.txt` 文件，内容：
```
paddlepaddle==3.2.0
paddleocr>=3.0
opencv-python-headless>=4.10
pillow>=10.0
numpy>=1.24
PyYAML>=6.0
tqdm>=4.0
```

2. 创建 `scripts/check_env.py` 脚本用于验证环境：
```python
#!/usr/bin/env python3
"""验证开发环境配置"""
import sys

def check_package(name, import_name=None, version_check=None):
    try:
        mod = __import__(import_name or name)
        version = getattr(mod, '__version__', 'unknown')
        if version_check and version_check not in version:
            return False, f"{name} version {version} (expected {version_check})"
        return True, f"{name} {version}"
    except ImportError as e:
        return False, f"{name} NOT FOUND: {e}"

checks = [
    check_package('paddlepaddle', 'paddlepaddle', '3.2'),
    check_package('paddleocr', 'paddleocr'),
    check_package('opencv', 'cv2', '4.'),
    check_package('pillow', 'PIL'),
    check_package('numpy', 'numpy'),
]

all_pass = True
for passed, msg in checks:
    status = "✓" if passed else "✗"
    print(f"{status} {msg}")
    if not passed:
        all_pass = False

sys.exit(0 if all_pass else 1)
```

3. 运行安装命令（使用 uv）：
```bash
uv pip install -r requirements.txt
```

或者通过 pyproject.toml 管理：
```bash
uv init
uv add paddlepaddle==3.2.0 paddleocr opencv-python-headless pillow numpy PyYAML tqdm
```

</action>

<acceptance_criteria>

- requirements.txt 包含 paddlepaddle==3.2.0, paddleocr, opencv-python-headless>=4.10, pillow>=10.0, numpy>=1.24
- `python scripts/check_env.py` 输出全部通过（5个 ✓）
- `python -c "from paddleocr import PaddleOCR; print('PaddleOCR OK')"` 或 `uv run python -c "from paddleocr import PaddleOCR; print('PaddleOCR OK')"` 无报错

</acceptance_criteria>

<verify>

```bash
python scripts/check_env.py
```

预期输出：5 个 ✓，退出码 0

</verify>

</task>

<task>
<read_first>
- .planning/research/STACK.md §Ubuntu 服务器环境注意事项
</read_first>

<action>

验证代码的 Ubuntu 服务器兼容性：

1. 检查所有依赖是否支持 Ubuntu：
   - opencv-python-headless: ✓ 支持 Linux
   - paddlepaddle: ✓ 支持 Linux (需要 glibc 2.31+)
   - paddleocr: ✓ 支持 Linux

2. 创建 `scripts/check_ubuntu_compat.py`：
```python
#!/usr/bin/env python3
"""Ubuntu 服务器兼容性检查"""
import sys
import platform

def check_ubuntu_compat():
    """检查 Ubuntu 兼容性"""
    issues = []

    # 检查操作系统
    if platform.system() != 'Linux':
        print(f"注意: 当前系统 {platform.system()}，Ubuntu 验证需在服务器上进行")
        print("代码兼容性检查：")
    else:
        print(f"当前系统: {platform.system()} {platform.release()}")

    # 检查 Python 版本
    if sys.version_info < (3, 8):
        issues.append("Python 版本需要 >= 3.8")
    print(f"✓ Python {sys.version_info.major}.{sys.version_info.minor}")

    # 检查关键依赖的跨平台性
    cross_platform_deps = [
        ('paddlepaddle', 'paddlepaddle'),
        ('paddleocr', 'paddleocr'),
        ('cv2', 'opencv-python-headless'),
        ('PIL', 'pillow'),
        ('numpy', 'numpy'),
    ]

    for mod_name, pkg_name in cross_platform_deps:
        try:
            __import__(mod_name)
            print(f"✓ {pkg_name} 已安装 (跨平台)")
        except ImportError:
            issues.append(f"{pkg_name} 未安装")

    # Ubuntu 特定检查
    if platform.system() == 'Linux':
        import subprocess
        try:
            result = subprocess.run(['ldd', '--version'], capture_output=True, text=True)
            ldd_version = result.stdout.split('\n')[0]
            print(f"✓ {ldd_version}")
        except Exception as e:
            issues.append(f"无法检查 glibc 版本: {e}")

    if issues:
        print("\n问题:")
        for issue in issues:
            print(f"  ✗ {issue}")
        return False
    else:
        print("\n✓ 代码兼容性检查通过")
        print("  实际 Ubuntu 部署验证请在服务器上运行:")
        print("  1. uv pip install -r requirements.txt")
        print("  2. python scripts/check_env.py")
        print("  3. paddleocr install_hpi_deps cpu  # 安装 MKL-DNN")
        return True

if __name__ == '__main__':
    success = check_ubuntu_compat()
    sys.exit(0 if success else 1)
```

3. 运行兼容性检查：
```bash
python scripts/check_ubuntu_compat.py
```

</action>

<acceptance_criteria>

- scripts/check_ubuntu_compat.py 存在
- 代码使用跨平台依赖（opencv-python-headless 而非 opencv-python）
- 不使用任何 Windows/macOS 特有 API
- 注释说明 Ubuntu 部署步骤

</acceptance_criteria>

<verify>

```bash
python scripts/check_ubuntu_compat.py
```

预期输出：显示兼容性检查结果和 Ubuntu 部署步骤

</verify>

</task>

</tasks>

<verification>

1. 运行 `python scripts/check_env.py` 确认所有依赖安装成功
2. 验证 Python 版本：`python --version` 输出 3.10 或 3.11
3. 确认 paddlepaddle 版本为 3.2.0：`uv run python -c "import paddlepaddle; print(paddlepaddle.__version__)"`

</verification>

<success_criteria>

- [ ] requirements.txt 创建完成
- [ ] 所有依赖安装成功（使用 uv）
- [ ] check_env.py 脚本运行通过
- [ ] PaddleOCR 可正常导入
- [ ] check_ubuntu_compat.py 脚本存在且显示兼容性通过

</success_criteria>